{
  "name": "syb2-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:smoke": "npx playwright test --project=smoke",
    "test:full": "npx playwright test --project=full",
    "test:ui": "npx playwright test --ui",
    "report": "npx playwright show-report",
    "screenshots": "npx tsx take-screenshots.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
